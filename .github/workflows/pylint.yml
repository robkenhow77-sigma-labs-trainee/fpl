name: Pylint

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: 'latest'  # Use the latest Python version available
    - name: Install dependencies
      run: echo helllo